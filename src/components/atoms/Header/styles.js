import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    backgroundColor: "#fff",
  },
  headerTitle: {
    color: "#1E40AF",
    textAlign: "center",
    fontWeight: "500",
    fontSize: "15",
    margin: "5",
  },
});

export default styles;
